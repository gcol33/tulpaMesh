% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/refine.R
\name{refine_mesh}
\alias{refine_mesh}
\title{Adaptively Refine a Mesh Based on Error Indicators}
\usage{
refine_mesh(
  mesh,
  indicators,
  threshold = NULL,
  fraction = NULL,
  max_iter = 1L,
  min_area = 0
)
}
\arguments{
\item{mesh}{A \code{tulpa_mesh} object (2D only).}

\item{indicators}{Numeric vector of per-triangle error indicators
(length \code{n_triangles}). Higher values trigger refinement.}

\item{threshold}{Triangles with indicator above this value are refined.
Default: \code{median(indicators)} (refine worst half).}

\item{fraction}{Alternative to \code{threshold}: refine this fraction of
triangles with the highest indicators. Default \code{NULL} (use threshold).}

\item{max_iter}{Maximum number of refine-retriangulate iterations.
Default 1 (single refinement pass).}

\item{min_area}{Minimum triangle area below which triangles are never
refined, regardless of indicator. Default 0 (no limit).}
}
\value{
A new \code{tulpa_mesh} object with refined triangulation.
}
\description{
Refines triangles where error indicators exceed a threshold by inserting
their centroids as new vertices and re-triangulating. Designed for
iterative solve-refine-re-solve workflows where error indicators come
from an SPDE solver (e.g., tulpa's posterior variance).
}
