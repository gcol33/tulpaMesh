% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/graph.R
\name{tulpa_mesh_graph}
\alias{tulpa_mesh_graph}
\alias{print.tulpa_mesh_graph}
\title{Create a Metric Graph Mesh}
\usage{
tulpa_mesh_graph(edges, max_edge = NULL, snap_tolerance = 1e-08)

\method{print}{tulpa_mesh_graph}(x, ...)
}
\arguments{
\item{edges}{A list of N x 2 coordinate matrices, each defining a
polyline edge of the network. Or an sf object with LINESTRING
geometries.}

\item{max_edge}{Maximum segment length along edges. Edges longer
than this are subdivided. Default \code{NULL} (use vertices as-is).}

\item{snap_tolerance}{Distance below which endpoints are snapped
to the same junction node. Default \code{1e-8}.}

\item{x}{A \code{tulpa_mesh_graph} object.}

\item{...}{Additional arguments (ignored).}
}
\value{
A \code{tulpa_mesh_graph} object with components:
\itemize{
\item \code{vertices}: N x 2 matrix of node coordinates
\item \code{segments}: M x 2 integer matrix of segment connectivity (1-based)
\item \code{n_vertices}, \code{n_segments}: counts
\item \code{C}: consistent mass matrix (tridiagonal-block sparse)
\item \code{G}: stiffness matrix (tridiagonal-block sparse)
\item \code{C0}: lumped (diagonal) mass matrix
\item \code{degree}: integer vector of node degrees (junction = degree > 2)
}

The \code{tulpa_mesh_graph} object \code{x}, returned invisibly.
}
\description{
Builds a 1D FEM mesh along the edges of a spatial network (roads,
rivers, coastlines). Each edge is discretized into segments with
P1 linear elements. Junction nodes where edges meet are shared.
Based on the Whittle-Matern SPDE formulation on metric graphs
(Bolin, Simas & Wallin, 2024).
}
