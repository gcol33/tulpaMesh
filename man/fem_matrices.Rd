% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mesh.R
\name{fem_matrices}
\alias{fem_matrices}
\title{Compute FEM Matrices from a Mesh}
\usage{
fem_matrices(
  mesh,
  obs_coords = NULL,
  barrier = NULL,
  parallel = FALSE,
  lumped = FALSE
)
}
\arguments{
\item{mesh}{A \code{tulpa_mesh} object.}

\item{obs_coords}{Observation coordinates (N x 2 matrix). If NULL, the
projection matrix A is the identity (observations at mesh nodes).}

\item{barrier}{Optional logical vector of length \code{n_triangles}. Triangles
marked \code{TRUE} are treated as physical barriers (coastlines, rivers):
their stiffness contributions are zeroed so the spatial field cannot
smooth across them. Based on Bakka et al. (2019).}

\item{parallel}{Logical. If \code{TRUE}, uses parallel FEM assembly via
RcppParallel (thread-local triplet accumulation). Beneficial for
meshes with >50K triangles. Default \code{FALSE}.}

\item{lumped}{Logical. If \code{TRUE}, returns a diagonal lumped mass matrix
C0 (vertex areas) in addition to the consistent mass matrix C. The
lumped mass inverse is trivial and needed for the SPDE Q-builder.
Default \code{FALSE}.}
}
\value{
A list with sparse matrices (dgCMatrix class from Matrix package):
\itemize{
\item \code{C}: consistent mass matrix (n_vertices x n_vertices)
\item \code{G}: stiffness matrix (n_vertices x n_vertices)
\item \code{A}: projection matrix (n_obs x n_vertices)
\item \code{n_mesh}: number of mesh vertices
\item \code{C0}: (only if \code{lumped = TRUE}) diagonal lumped mass matrix
\item \code{va}: (only if \code{lumped = TRUE}) vertex areas (numeric vector)
\item \code{ta}: (only if \code{lumped = TRUE}) triangle areas (numeric vector)
}
}
\description{
Computes the finite element mass (C) and stiffness (G) matrices from
a triangular mesh, plus the projection matrix (A) that maps mesh
vertices to observation locations.
}
