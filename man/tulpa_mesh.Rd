% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mesh.R, R/sphere.R
\name{tulpa_mesh}
\alias{tulpa_mesh}
\alias{print.tulpa_mesh}
\title{Create a Triangular Mesh for SPDE Spatial Models}
\usage{
tulpa_mesh(
  coords,
  data = NULL,
  boundary = NULL,
  max_edge = NULL,
  cutoff = 0,
  extend = 0.1,
  min_angle = NULL,
  max_area = NULL,
  max_steiner = 10000L
)

\method{print}{tulpa_mesh}(x, ...)
}
\arguments{
\item{coords}{A matrix or data.frame with columns x and y, or a formula
like \code{~ x + y} evaluated in \code{data}.}

\item{data}{Optional data.frame for formula evaluation.}

\item{boundary}{Optional boundary specification: a matrix of boundary
vertex coordinates (N x 2), an sf polygon, or NULL for convex hull.}

\item{max_edge}{Maximum edge length. A single value or a vector of two
values \code{c(inner, outer)} where \code{inner} controls the study area and
\code{outer} controls the extension region.}

\item{cutoff}{Minimum distance between mesh vertices. Points closer than
this are merged. Default 0 (no merging).}

\item{extend}{Numeric extension factor beyond the boundary. Default 0.1
(10\% of domain diameter). Set to 0 for no extension.}

\item{min_angle}{Minimum angle (degrees) for Ruppert refinement. If
specified, Steiner points are inserted at circumcenters of triangles
with angles below this threshold. Theoretical maximum is ~20.7 degrees;
values up to 30 usually work. Default \code{NULL} (no refinement).}

\item{max_area}{Maximum triangle area for refinement. Triangles larger
than this are refined regardless of angle quality. Default \code{NULL}
(no area constraint).}

\item{max_steiner}{Maximum number of Steiner points to insert during
Ruppert refinement. Default 10000.}

\item{x}{A \code{tulpa_mesh} object.}

\item{...}{Additional arguments (ignored).}
}
\value{
A \code{tulpa_mesh} object with components:
\itemize{
\item \code{vertices}: N x 2 matrix of vertex coordinates
\item \code{triangles}: M x 3 integer matrix of vertex indices (1-based)
\item \code{edges}: K x 2 integer matrix of edge vertex indices (1-based)
\item \code{n_vertices}: number of mesh vertices
\item \code{n_triangles}: number of triangles
\item \code{n_edges}: number of edges
\item \code{n_input_points}: number of original input points
}

The \code{tulpa_mesh} object \code{x}, returned invisibly.
}
\description{
Generates a constrained Delaunay triangulation from point coordinates,
optionally with boundary constraints. The resulting mesh can be used
directly with tulpa's SPDE spatial fields.
}
\examples{
# Simple mesh from random points
set.seed(42)
coords <- cbind(runif(50), runif(50))
mesh <- tulpa_mesh(coords)
print(mesh)

# Mesh with Ruppert refinement (min angle 25 degrees)
mesh_refined <- tulpa_mesh(coords, min_angle = 25)
}
